# Go build output
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go test binary
*.test

# Go coverage
*.out
*.prof

# Vendor (if not committed)
vendor/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# macOS
.DS_Store

# Project secrets and local config
.env
secrets/localhost.env
secrets/localhost.yaml
config/localhost.yaml

# Help-text staging copies (canonical sources live in docs/help/;
# Makefile copies them into each binary's package dir at build time
# so Go's //go:embed can reach them).
cmd/meet-helper/help.txt
